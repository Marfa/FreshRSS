# Managed by deploy/blue-green-deploy.sh — do not edit by hand on the VPS.
# Included from sites-enabled/freshrss (defines upstream freshrss_upstream).
upstream freshrss_upstream {
	server 127.0.0.1:8083;
	keepalive 8;
}
