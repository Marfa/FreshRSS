# Optional. Only needed for first-time FreshRSS install via entrypoint.
# Existing feeds.themarfa.name instance already has data in volume freshrss_data.

# FRESHRSS_INSTALL=--api-enabled --base-url https://feeds.themarfa.name --default-user admin --language ru
# FRESHRSS_USER=--api-password CHANGE_ME --email admin@example.com --language ru --password CHANGE_ME --user admin
